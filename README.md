# README.me
Automated security script built in Python to audit network assets for open ports &amp; vulnerabilities. Transforms raw telemetry into structured JSON logs and securely forwards alerts to a Splunk SIEM via the HTTP Event Collector (HEC) API. Engineered within a PEP 668 virtual environment with robust exception handling for production pipelines.
